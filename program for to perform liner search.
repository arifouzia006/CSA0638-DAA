def linearSearch(arr, target):
    for i in range(len(arr)):
        if arr[i] == target:
            return i
    return -1

# Test the function
arr = [1, 2, 3, 4, 5]
target = 4
print(linearSearch(arr, target))
